import 'package:flutter/material.dart';

class CreateAPlanPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text("CreateAPlan")),
      body: Center(child: Text("CreateAPlan Page")),
    );
  }
}
