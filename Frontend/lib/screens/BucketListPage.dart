import 'package:flutter/material.dart';

class BucketListPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text("BucketList")),
      body: Center(child: Text("Bucket List Page")),
    );
  }
}
